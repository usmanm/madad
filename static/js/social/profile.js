function like_activity(btn) {
  id = $(btn).attr('data-id');
  console.log(id);
}

function like_user(btn) {
  id = $(btn).attr('data-id');
  console.log("Like + " + id);
}

function like_user(btn) {
  id = $(btn).attr('data-id');
  console.log("Unlike + " + id);
}